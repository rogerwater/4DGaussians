TASK: Change MPC optimization iteration defaults from 5 to 10 in three files

VERIFICATION RESULTS:
=====================

FILE 1: test_cotracker_mpc.py (Line 256)
=========================================
BEFORE: parser.add_argument("--opt_iters", type=int, default=5, help="CEM optimization iterations")
AFTER:  parser.add_argument("--opt_iters", type=int, default=10, help="CEM optimization iterations")
STATUS: ✓ VERIFIED - default changed to 10

FILE 2: demo_flow_guided_mpc.py (Multiple locations)
====================================================
Location 1 (Line 426 - setup_flow_guided_cem function):
  BEFORE: opt_iters: int = 5,
  AFTER:  opt_iters: int = 10,
  STATUS: ✓ VERIFIED - default changed to 10

Location 2 (Line 542 - run_mpc_from_images function):
  BEFORE: opt_iters: int = 5,
  AFTER:  opt_iters: int = 10,
  STATUS: ✓ VERIFIED - default changed to 10

Location 3 (Line 1394 - argparse):
  BEFORE: parser.add_argument('--opt_iters', type=int, default=5,
  AFTER:  parser.add_argument('--opt_iters', type=int, default=10,
  STATUS: ✓ VERIFIED - default changed to 10

FILE 3: demo_cotracker_mpc.py (Line 81)
========================================
BEFORE: opt_iters: int = 5,
AFTER:  opt_iters: int = 10,
STATUS: ✓ VERIFIED - default changed to 10

HELP TEXT VERIFICATION:
======================
test_cotracker_mpc.py --help:
  --opt_iters OPT_ITERS
                        CEM optimization iterations
  (Default 10 configured in argparse)

demo_flow_guided_mpc.py --help:
  --opt_iters OPT_ITERS
                        CEM优化迭代数
  (Default 10 configured in argparse)

GREP VERIFICATION:
==================
$ grep "opt_iters.*default=10" test_cotracker_mpc.py
    parser.add_argument("--opt_iters", type=int, default=10, help="CEM optimization iterations")
    ✓ PASS

$ grep "opt_iters.*default=10" demo_flow_guided_mpc.py
    parser.add_argument('--opt_iters', type=int, default=10,
    ✓ PASS

$ grep "opt_iters: int = 10" demo_cotracker_mpc.py
    opt_iters: int = 10,
    ✓ PASS

SUMMARY:
========
✓ All 5 default value changes completed successfully
✓ test_cotracker_mpc.py: 1 argparse default changed (5→10)
✓ demo_flow_guided_mpc.py: 3 defaults changed (2 function parameters + 1 argparse)
✓ demo_cotracker_mpc.py: 1 function parameter changed (5→10)
✓ No other defaults modified
✓ Help text strings preserved (no changes to text)
✓ Code style preserved (research codebase conventions maintained)

RATIONALE:
==========
Increased default from 5 to 10 iterations to reduce local optima in CEM optimization,
as confirmed by user requirements for improved MPC planning quality.

TASK STATUS: COMPLETE ✓
